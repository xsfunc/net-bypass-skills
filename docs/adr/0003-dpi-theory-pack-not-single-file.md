# dpi-tspu-strategy-theory is a pack, not a single file (overrides PRD #28)

PRD #28 specified `dpi-tspu-strategy-theory` as a single SKILL.md. We amend it to a pack: a lean SKILL.md index + 4 reference cards (`siberian-scheme`, `ja3-ja4`, `ttl-detection`, `hypothesis-frontier`). The seam is epistemic: `hypothesis-frontier.md` isolates the entire `[evidence: hypothesis]` tier (eternal-h2/h3 mechanism, statistical morphing, TLS-in-TLS round-trip, SNI↔IP mismatch) from the verified+community-observed core, so activation stays lean and speculation is gated at the file level (loaded only on explicit demand) rather than only by an inline tag. This mirrors the #5 amendment that promoted `zapret2-router-deploy` from single-file to pack for the same bloat-avoidance reason, and follows the repo's pack convention (lean index + reference cards) over the single-file exception. Revisit if the core cards distil to <30 lines each.
